{
  "name": "@aigency/docs",
  "version": "0.1.0",
  "private": true,
  "description": "Aigency Documentation — deep-wiki, onboarding guides, and AGENTS.md rendered as a deployable static site",
  "type": "module",
  "scripts": {
    "build": "echo 'Docs site build not yet implemented — see README roadmap'",
    "clean": "rimraf dist",
    "dev": "echo 'Docs dev server not yet implemented — see README roadmap'",
    "lint": "echo 'No lint configured yet'",
    "typecheck": "echo 'No TypeScript to check yet'"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
