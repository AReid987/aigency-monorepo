#!/usr/bin/env node
/**
 * TELOS CLI — future entrypoint
 *
 * Planned commands:
 *   telos interview --agent <callsign>
 *   telos validate <file>
 *   telos status
 *   telos render <file> --output <html>
 *   telos build-site
 *   telos dev
 */

console.log("TELOS CLI v0.1.0 — placeholder");
console.log("See apps/telos/README.md for roadmap.");
process.exit(0);
