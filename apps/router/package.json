{
  "name": "@aigency/router",
  "version": "0.2.0",
  "private": true,
  "description": "Aigency LLM Router — OpenAI-compatible proxy with quota-aware routing, local SLM support, and agent-identity context",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsup src/server.ts src/cli.ts --format esm --dts",
    "clean": "rimraf dist",
    "dev": "tsx watch src/server.ts",
    "lint": "biome check src",
    "start": "node dist/server.js",
    "test": "jest --passWithNoTests",
    "test:coverage": "jest --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aigency/agent-core": "workspace:*",
    "dotenv": "^16.4.0",
    "js-yaml": "^4.1.0",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@aigency/tsconfig": "workspace:*",
    "@types/jest": "^29.5.14",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.2",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "yaml": "^2.6.1"
  }
}
